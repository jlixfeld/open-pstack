# Changed branch coverage rubric

Accept only answers that inspect uncovered changed branches and ask for a caller-visible protected outcome with a failing signal. Reject percentage-padding, execute-only, implementation-mirroring, and mock-only proof. When tooling is absent, require `unavailable` with `no-tooling` and substitute verification. Do not accept installing a coverage dependency.
